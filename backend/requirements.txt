fastapi
uvicorn[standard]
pydantic
python-dotenv
firebase-admin
python-multipart
numpy
opencv-python-headless
Pillow
scipy
face-recognition
resemblyzer
torch
librosa
soundfile

# Dementia action monitoring (YOLO pose + LSTM + live sessions on /api/dementia-action/*)
-r requirements-dementia-action.txt

# Cognitive screening prototype (/api/predict, /api/start-session, MRI, confusion frame)
-r requirements-cognitive-screening.txt

# Dementia Guardian (auth, pairing, alerts, clinical/MRI inference, Socket.IO) — needs MONGO_URI
-r requirements-guardian.txt
